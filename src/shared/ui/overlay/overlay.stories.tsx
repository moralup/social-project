import type { Meta, StoryObj } from '@storybook/react';
import { Overlay } from './overlay';

const meta: Meta<typeof Overlay> = {
    title: 'DIRECTORY/Overlay',
    component: Overlay,
    args: {

    },
};

export default meta;

type Story = StoryObj<typeof Overlay>;

export const Primary: Story = {

};
